//2.16

#include <iostream>

int main()
{
	using std::cin;
	using std::cout;
	float a, b;
	cout << "please input two numbers :";
	cin >> a >> b;
	cout << "a + b = " << a + b << "\na * b = " << a * b << "\na - b = " << a - b << "\na / b = " << a / b << "\n";
	return 0;
}
